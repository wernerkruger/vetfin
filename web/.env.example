# VetFin API base URL
#
# Local dev (Vite on :5173, API on :3001):
VITE_API_URL=http://localhost:3001
#
# Docker / AWS (nginx proxies /api to the backend on the same host):
# Leave empty or omit — the built app uses same-origin /api requests.
# VITE_API_URL=
